/**
 * This file should only be used so that we can
 * define custom main file in package.json instead
 * of node_modules/expo/AppEntry.js
 */
import App from './App';
import { registerRootComponent } from 'expo';
registerRootComponent(App);
