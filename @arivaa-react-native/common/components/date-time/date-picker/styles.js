import React from 'react-native';

var styles = React.StyleSheet.create({
  view: {
    borderWidth: 1,
    borderColor: '#e9e9e9',
    marginTop: 10,
    marginBottom: 10,
  },
});

export default styles;
